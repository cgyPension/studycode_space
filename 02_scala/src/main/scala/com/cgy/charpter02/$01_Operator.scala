package com.cgy.charpter02

/**
 * java中运算符
 *   1、算符运算符 ++ --
 *   2、赋值运算符 += -=
 *   3、逻辑运算符
 *   4、位运算符
 *   5、关系运算符
 *   6、三目运算符:   1==1 ? 0 : 2
 * scala
 *   1、算术运算符: + - * / %
 *   2、赋值运算符: = 、+=..
 *   3、逻辑运算符: && || !
 *   4、位运算符: & | >> >>>
 *   5、关系运算符: >、<、>=、<=、==、!=
 * scala中没有三目运算符
 * scala中没有++ 与 --
 */

object $01_Operator {

  def main(args: Array[String]): Unit = {
    /**
     * int i=0
     * i = i++
     * i = i++
     * i = i++
     * sout(i)
     * int j=10
     * j=++j
     * j=++j
     * j=++j
     * sout(j)
     */

  }

}
